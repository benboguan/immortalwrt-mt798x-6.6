--- a/Makefile
+++ b/Makefile
@@ -32,6 +32,11 @@ ifneq ($(filter madwifi,$(IWINFO_BACKEND
 	IWINFO_LIB_OBJ += iwinfo_madwifi.o
 endif
 
+ifneq ($(filter mtk,$(IWINFO_BACKENDS)),)
+	IWINFO_CFLAGS  += -DUSE_MTK
+	IWINFO_LIB_OBJ += iwinfo_mtk.o iwinfo_mtk_rate.o iwinfo_mtk_l1util.o
+endif
+
 ifneq ($(filter nl80211,$(IWINFO_BACKENDS)),)
 	IWINFO_CFLAGS      += -DUSE_NL80211
 	IWINFO_CLI_LDFLAGS += -lnl-tiny
--- a/include/iwinfo.h
+++ b/include/iwinfo.h
@@ -243,6 +243,7 @@ struct iwinfo_rate_entry {
 	uint8_t nss;
 	uint8_t mhz_hi;
 	uint8_t eht_gi;
+	uint8_t eht_dcm;
 };
 
 struct iwinfo_assoclist_entry {
@@ -275,6 +276,10 @@ struct iwinfo_assoclist_entry {
 	char local_ps[16];
 	char peer_ps[16];
 	char nonpeer_ps[16];
+	//struct iwinfo_rate_entry mlo_rx_rate;
+	//struct iwinfo_rate_entry mlo_tx_rate;
+	uint8_t mlo_valid:1;
+	uint8_t	mlo_addr[6];
 };
 
 struct iwinfo_survey_entry {
@@ -328,8 +334,9 @@ extern const uint16_t ht_chan
 /* 0 = 40 MHz or lower (refer to ht to a more precise width)
    1 = 80 MHz
    2 = 160 MHz
-   3 = 80+80 MHz */
+   3 = 80+80 MHz
+   4 = 320 MHz */
-extern const uint16_t vht_chan_width[4];
+extern const uint16_t vht_chan_width[5];
 
 struct iwinfo_scanlist_entry {
 	uint8_t mac[6];
@@ -429,6 +435,7 @@ extern const struct iwinfo_ops wext_ops;
 extern const struct iwinfo_ops madwifi_ops;
 extern const struct iwinfo_ops nl80211_ops;
 extern const struct iwinfo_ops wl_ops;
+extern const struct iwinfo_ops mtk_ops;
 
 #include "iwinfo/utils.h"
 
--- a/iwinfo_lib.c
+++ b/iwinfo_lib.c
@@ -126,11 +126,12 @@ const uint16_t ht_chan_width[2] = {
 	2040,
 };
 
-const uint16_t vht_chan_width[4] = {
+const uint16_t vht_chan_width[5] = {
 	40, /* 40 MHz or lower (refer to ht to a more precise width) */
 	80, /* 80 MHz */
 	160, /* 160 MHz */
 	8080, /* 80+80 MHz */
+	320, /* 320 MHz */
 };
 
 /*
@@ -401,6 +402,9 @@ static const struct iwinfo_ops *backends
 #ifdef USE_WEXT
 	&wext_ops,
 #endif
+#ifdef USE_MTK
+	&mtk_ops,
+#endif
 };
 
 const char * iwinfo_type(const char *ifname)
--- a/include/iwinfo/lua.h
+++ b/include/iwinfo/lua.h
@@ -41,6 +41,9 @@
 #define IWINFO_NL80211_META	"iwinfo.nl80211"
 #endif
 
+#ifdef USE_MTK
+#define IWINFO_MTK_META	"iwinfo.mtk"
+#endif
 
 #define LUA_REG(type,op) \
 	{ #op, iwinfo_L_##type##_##op }
--- a/iwinfo_cli.c
+++ b/iwinfo_cli.c
@@ -337,6 +337,9 @@ static char * format_assocrate(struct
 
 			p += snprintf(p, l, ", EHT-GI %d", r->eht_gi);
 			l = sizeof(buf) - (p - buf);
+
+			p += snprintf(p, l, ", EHT-DCM %d", r->eht_dcm);
+			l = sizeof(buf) - (p - buf);
 		}
 	}
 
@@ -357,6 +360,7 @@ static const char* format_chan_width(bool
 			case 80: return "80 MHz";
 			case 8080: return "80+80 MHz";
 			case 160: return "160 MHz";
+			case 320: return "320 MHz";
 		}
 
 	return "unknown";
@@ -709,7 +713,7 @@ static void print_scanlist(const
 			format_chan_width(false, e->ht_chan_info.chan_width));
 
 		if (e->vht_chan_info.center_chan_1) {
-			printf("          VHT Operation:\n");
+			printf("          VHT/HE/EHT Operation:\n");
 			printf("                    Center Frequency 1: %d\n",
 				 e->vht_chan_info.center_chan_1);
 			printf("                    Center Frequency 2: %d\n",
@@ -820,6 +824,20 @@ static void print_assoclist(const
 			e->tx_packets
 		);
 
+		if (e->mlo_valid)
+		{
+			printf("	mldAddr=%s\n",
+				format_bssid(e->mlo_addr));
+
+			/* printf("	RX: %-38s \n",
+				format_assocrate(&e->mlo_rx_rate)
+			);
+			
+			printf("	TX: %-38s \n",
+				format_assocrate(&e->mlo_tx_rate)
+			); */
+		}
+
 		printf("	expected throughput: %s\n\n",
 			format_rate(e->thr));
 	}
--- a/iwinfo_lua.c
+++ b/iwinfo_lua.c
@@ -315,6 +315,9 @@ static void set_rateinfo(lua_State
 		if (r->is_eht) {
 			lua_pushnumber(L, r->eht_gi);
 			lua_setfield(L, -2, rx ? "rx_eht_gi" : "tx_eht_gi");
+
+			lua_pushnumber(L, r->eht_dcm);
+			lua_setfield(L, -2, rx ? "rx_eht_dcm" : "tx_eht_dcm");
 		}
 
 		if (r->is_vht) {
@@ -342,6 +345,11 @@ static int iwinfo_L_assoclist(lua_State *L
 		{
 			e = (struct iwinfo_assoclist_entry *) &rv[i];
 
+			if(e->mlo_valid)
+				sprintf(macstr, "%02X:%02X:%02X:%02X:%02X:%02X",
+					e->mlo_addr[0], e->mlo_addr[1], e->mlo_addr[2],
+					e->mlo_addr[3], e->mlo_addr[4], e->mlo_addr[5]);
+			else
 			sprintf(macstr, "%02X:%02X:%02X:%02X:%02X:%02X",
 				e->mac[0], e->mac[1], e->mac[2],
 				e->mac[3], e->mac[4], e->mac[5]);
@@ -363,9 +371,36 @@ static int iwinfo_L_assoclist(lua_State *L
 			lua_pushnumber(L, e->tx_packets);
 			lua_setfield(L, -2, "tx_packets");
 
+			lua_pushnumber(L, e->rx_bytes);
+			lua_setfield(L, -2, "rx_bytes");
+
+			lua_pushnumber(L, e->tx_bytes);
+			lua_setfield(L, -2, "tx_bytes");
+
 			set_rateinfo(L, &e->rx_rate, true);
 			set_rateinfo(L, &e->tx_rate, false);
 
+			if (e->mlo_valid)
+			{
+				lua_pushboolean(L, e->mlo_valid);
+				lua_setfield(L, -2, "mlo_valid");
+
+				/* lua_pushnumber(L, e->mlo_rx_rate.rate);
+				lua_setfield(L, -2, "mlo_rx_rate");
+	
+				lua_pushnumber(L, e->mlo_tx_rate.rate);
+				lua_setfield(L, -2, "mlo_tx_rate"); */
+
+				lua_pushboolean(L, 1);
+				lua_setfield(L, -2, "ismlo");
+			}
+			else
+			{
+				lua_pushboolean(L, 0);
+				lua_setfield(L, -2, "ismlo");
+
+			}
+
 			if (e->thr) {
 				lua_pushnumber(L, e->thr);
 				lua_setfield(L, -2, "expected_throughput");
@@ -788,6 +839,36 @@ LUA_WRAP_STRUCT_OP(nl80211,mbssid_suppor
 LUA_WRAP_STRUCT_OP(nl80211,hardware_id)
 #endif
 
+#ifdef USE_MTK
+/* MTK */
+LUA_WRAP_INT_OP(mtk,channel)
+LUA_WRAP_INT_OP(mtk,frequency)
+LUA_WRAP_INT_OP(mtk,frequency_offset)
+LUA_WRAP_INT_OP(mtk,txpower)
+LUA_WRAP_INT_OP(mtk,txpower_offset)
+LUA_WRAP_INT_OP(mtk,bitrate)
+LUA_WRAP_INT_OP(mtk,signal)
+LUA_WRAP_INT_OP(mtk,noise)
+LUA_WRAP_INT_OP(mtk,quality)
+LUA_WRAP_INT_OP(mtk,quality_max)
+LUA_WRAP_STRING_OP(mtk,ssid)
+LUA_WRAP_STRING_OP(mtk,bssid)
+LUA_WRAP_STRING_OP(mtk,country)
+LUA_WRAP_STRING_OP(mtk,hardware_name)
+LUA_WRAP_STRING_OP(mtk,phyname)
+LUA_WRAP_STRUCT_OP(mtk,mode)
+LUA_WRAP_STRUCT_OP(mtk,assoclist)
+LUA_WRAP_STRUCT_OP(mtk,txpwrlist)
+LUA_WRAP_STRUCT_OP(mtk,scanlist)
+LUA_WRAP_STRUCT_OP(mtk,freqlist)
+LUA_WRAP_STRUCT_OP(mtk,countrylist)
+LUA_WRAP_STRUCT_OP(mtk,hwmodelist)
+LUA_WRAP_STRUCT_OP(mtk,htmodelist)
+LUA_WRAP_STRUCT_OP(mtk,encryption)
+LUA_WRAP_STRUCT_OP(mtk,mbssid_support)
+LUA_WRAP_STRUCT_OP(mtk,hardware_id)
+#endif
+
 /* Wext */
 #ifdef USE_WEXT
 LUA_WRAP_INT_OP(wext,channel)
@@ -917,6 +998,39 @@ static const luaL_reg R_nl80211[] = {
 };
 #endif
 
+#ifdef USE_MTK
+/* MTK table */
+static const luaL_reg R_mtk[] = {
+	LUA_REG(mtk,channel),
+	LUA_REG(mtk,frequency),
+	LUA_REG(mtk,frequency_offset),
+	LUA_REG(mtk,txpower),
+	LUA_REG(mtk,txpower_offset),
+	LUA_REG(mtk,bitrate),
+	LUA_REG(mtk,signal),
+	LUA_REG(mtk,noise),
+	LUA_REG(mtk,quality),
+	LUA_REG(mtk,quality_max),
+	LUA_REG(mtk,mode),
+	LUA_REG(mtk,ssid),
+	LUA_REG(mtk,bssid),
+	LUA_REG(mtk,country),
+	LUA_REG(mtk,assoclist),
+	LUA_REG(mtk,txpwrlist),
+	LUA_REG(mtk,scanlist),
+	LUA_REG(mtk,freqlist),
+	LUA_REG(mtk,countrylist),
+	LUA_REG(mtk,hwmodelist),
+	LUA_REG(mtk,htmodelist),
+	LUA_REG(mtk,encryption),
+	LUA_REG(mtk,mbssid_support),
+	LUA_REG(mtk,hardware_id),
+	LUA_REG(mtk,hardware_name),
+	LUA_REG(mtk,phyname),
+	{ NULL, NULL }
+};
+#endif
+
 /* Wext table */
 #ifdef USE_WEXT
 static const luaL_reg R_wext[] = {
@@ -988,6 +1102,15 @@ LUALIB_API int luaopen_iwinfo(lua_State
 	lua_setfield(L, -2, "nl80211");
 #endif
 
+#ifdef USE_MTK
+	luaL_newmetatable(L, IWINFO_MTK_META);
+	luaL_register(L, NULL, R_common);
+	luaL_register(L, NULL, R_mtk);
+	lua_pushvalue(L, -1);
+	lua_setfield(L, -2, "__index");
+	lua_setfield(L, -2, "mtk");
+#endif
+
 #ifdef USE_WEXT
 	luaL_newmetatable(L, IWINFO_WEXT_META);
 	luaL_register(L, NULL, R_common);
--- a/devices.txt
+++ b/devices.txt
@@ -188,6 +188,12 @@
 0x14c3 0x7628 0x14c3 0x0004    0      0  "MediaTek" "MT76x8"
 0x14c3 0x7650 0x14c3 0x7650    0      0  "MediaTek" "MT7610E"
 0x14c3 0x7662 0x14c3 0x7662    0      0  "MediaTek" "MT76x2E"
+0x14c3 0x7986 0x14c3 0x7986    0      0  "MediaTek" "MT7986"
+0x14c3 0x7981 0x14c3 0x7981    0      0  "MediaTek" "MT7981"
+0x14c3 0x7916 0x14c3 0x7916    0      0  "MediaTek" "MT7916AN"
+0x14c3 0x7990 0x14c3 0x7990    0      0  "MediaTek" "MT7996E"
+0x14c3 0x7992 0x14c3 0x7992    0      0  "MediaTek" "MT7992E"
+0x14c3 0x7993 0x14c3 0x7993    0      0  "MediaTek" "MT7993E"
 0x14c3 0x7915 0x14c3 0x7915    0      0  "MediaTek" "MT7915E"
 0x14c3 0x7906 0x14c3 0x7906    0      0  "MediaTek" "MT7916AN"
 0x14c3 0x7925 0x1a3b 0x6002    0      0  "MediaTek" "MT7925E"
--- a/iwinfo_utils.c
+++ b/iwinfo_utils.c
@@ -340,6 +340,7 @@ int iwinfo_hardware_id_from_mtd(struct i
 {
 	FILE *mtd;
 	uint16_t *bc;
+	uint16_t ident;
 
 	int fd, off;
 	unsigned int len;
@@ -352,7 +353,7 @@ int iwinfo_hardware_id_from_mtd(struct i
 	{
 		if (fscanf(mtd, "mtd%d: %x %*x %127s", &off, &len, buf) < 3 ||
 		    (strcmp(buf, "\"boardconfig\"") && strcmp(buf, "\"EEPROM\"") &&
-		     strcmp(buf, "\"factory\"")))
+		     strcmp(buf, "\"factory\"") && strcmp(buf, "\"Factory\"")))
 		{
 			off = -1;
 			continue;
@@ -371,6 +372,38 @@ int iwinfo_hardware_id_from_mtd(struct i
 	if ((fd = open(buf, O_RDONLY)) < 0)
 		return -1;
 
+	if (read(fd, &ident, sizeof(ident)) != -1)
+	{
+		if (ident == 0x7981 || ident == 0x7986 || ident == 0x7916 || ident == 0x7990 || ident == 0x7992 || ident == 0x7993
+			|| ident == 0x8179 || ident == 0x8679 || ident == 0x1679 || ident == 0x9079 || ident == 0x9279 || ident == 0x9379)
+		{
+			uint16_t ident_zone2 = 0;
+			if (len > 0xa0002 && 
+				lseek(fd, 0xa0000, SEEK_SET) != -1 &&
+				read(fd, &ident_zone2, sizeof(ident_zone2)) != -1)
+			{
+				if (ident_zone2 != 0xFFFF && ident_zone2 != 0x0) {
+					close(fd);
+					return -1;
+				}
+			}
+			if ((ident & 0xff) == 0x79)
+				id->device_id = (ident >> 8) | (ident & 0x00ff) << 8;
+			else
+				id->device_id = ident;
+			id->vendor_id = 0x14c3;
+			id->subsystem_vendor_id = 0x14c3;
+			id->subsystem_device_id = id->device_id;
+			close(fd);
+			return 0;
+		} else if (ident == 0xffff || ident == 0x0) {
+			/* eeprom invalid */
+			close(fd);
+			return -1;
+		}
+	}
+	lseek(fd, 0, SEEK_SET);
+
 	bc = mmap(NULL, len, PROT_READ, MAP_PRIVATE|MAP_LOCKED, fd, 0);
 
 	if ((void *)bc != MAP_FAILED)
