--- a/hostapd/config_file.c
+++ b/hostapd/config_file.c
@@ -3591,7 +3591,7 @@ static int hostapd_config_fill(struct hostapd_config *conf,
 			conf->conf_changed = 1;
 		} else if (os_strncmp(pos, "vht:", 4) == 0) {
 			val = atoi(pos + 4);
-			if (val < 0 || val > 9) {
+			if (val < 0 || val > 11) {
 				wpa_printf(MSG_ERROR,
 					   "Line %d: invalid beacon_rate VHT-MCS %d",
 					   line, val);
