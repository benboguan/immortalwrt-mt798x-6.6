diff -urN backports-6.18.original/net/wireless/core.c backports-6.18/net/wireless/core.c
--- backports-6.18.original/net/wireless/core.c	2023-12-11 17:55:44.771076000 +0530
+++ backports-6.18/net/wireless/core.c	2023-12-12 14:01:41.374081000 +0530
@@ -1561,9 +1561,13 @@
 		break;
 	case NETDEV_REGISTER:
 		if (!wdev->registered) {
-			guard(wiphy)(&rdev->wiphy);
-
-			cfg80211_register_wdev(rdev, wdev);
+			if (wdev->iftype == NL80211_IFTYPE_AP_VLAN ) {
+				cfg80211_register_wdev(rdev, wdev);
+			} else {
+				guard(wiphy)(&rdev->wiphy);
+
+				cfg80211_register_wdev(rdev, wdev);
+			}
 		}
 		break;
 	case NETDEV_UNREGISTER:
@@ -1575,9 +1576,13 @@
 		 * so check wdev->registered.
 		 */
 		if (wdev->registered && !wdev->registering) {
-			guard(wiphy)(&rdev->wiphy);
-
-			_cfg80211_unregister_wdev(wdev, false);
+			if (wdev->iftype == NL80211_IFTYPE_AP_VLAN) {
+				_cfg80211_unregister_wdev(wdev, false);
+			} else {
+				guard(wiphy)(&rdev->wiphy);
+
+				_cfg80211_unregister_wdev(wdev, false);
+			}
 		}
 		break;
 	case NETDEV_GOING_DOWN:
