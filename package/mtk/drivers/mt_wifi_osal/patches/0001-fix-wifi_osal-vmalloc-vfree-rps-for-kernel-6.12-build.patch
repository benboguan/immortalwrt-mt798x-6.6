--- a/inc/mt_osal_mem.h
+++ b/inc/mt_osal_mem.h
@@ -4,6 +4,8 @@
 #ifndef __MT_OSAL_MEM_H_
 #define __MT_OSAL_MEM_H_
 
+#include <linux/vmalloc.h>
+
 static inline int mt_os_mem_alloc(void **mem, size_t size, bool allow_suspend)
 {
 	if (!mem)
--- a/mt_osal_packet.c
+++ b/mt_osal_packet.c
@@ -25,6 +25,9 @@
 #if (KERNEL_VERSION(6, 6, 0) <= LINUX_VERSION_CODE)
 #include <net/netdev_rx_queue.h>
 #endif
+#if (KERNEL_VERSION(6, 12, 0) <= LINUX_VERSION_CODE)
+#include <net/rps.h>
+#endif
 
 #define SUBMODULE "mt_osal_packet"
 #include <mt_osal_debug.h>
--- a/mt_osal_net.c
+++ b/mt_osal_net.c
@@ -21,6 +21,7 @@
 #include <linux/wireless.h>
 #include <linux/netdevice.h>
 #include <linux/etherdevice.h>
+#include <linux/vmalloc.h>
 
 #define SUBMODULE "mt_osal_net"
 #include <mt_osal_debug.h>
@@ -133,7 +134,7 @@ static bool mt_os_net_dev_request_name(
 
 	if (ifNameIdx < 32) {
 		*ioctl_if = ifNameIdx;
-		strlcpy(&net_dev->name[0], &desiredName[0], sizeof(net_dev->name));
+		strncpy(&net_dev->name[0], &desiredName[0], sizeof(net_dev->name));
 		return true;
 	} else {
 		MT_DEBUG_TRACE(
@@ -550,7 +551,7 @@ static bool mt_os_net_dev_request_name(
 	/* find a available interface name, max 32 interfaces */
 	if (!add_suffix) {
 		memset(&net_dev->name[0], 0, IFNAMSIZ);
-		strlcpy(&net_dev->name[0], name_prefix, sizeof(net_dev->name));
+		strncpy(&net_dev->name[0], name_prefix, sizeof(net_dev->name));
 	} else {
 		status = mt_os_net_dev_request_name(
 			mc_row_id, ioctl_if, net_dev, name_prefix, dev_num, auto_suffix);
@@ -699,7 +700,12 @@ int mt_init_dummy_netdev(struct
 
 int mt_init_dummy_netdev(struct net_device *dev)
 {
+#if (KERNEL_VERSION(6, 12, 0) <= LINUX_VERSION_CODE)
+	init_dummy_netdev(dev);
+	return 0;
+#else
 	return init_dummy_netdev(dev);
+#endif
 }
 EXPORT_SYMBOL(mt_init_dummy_netdev);
 
